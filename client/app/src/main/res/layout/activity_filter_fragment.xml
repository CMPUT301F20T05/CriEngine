<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:layout_centerVertical="false"
    android:layout_marginLeft="100dp"
    android:orientation="vertical">

    <CheckBox
        android:id="@+id/checkbox_available_filter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="25dp"
        android:text="@string/filter_available_button"
        android:textSize="24sp" />

    <CheckBox
        android:id="@+id/checkbox_requests_filter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="@string/filter_requests_button"
        android:textSize="24sp" />

    <CheckBox
        android:id="@+id/checkbox_accepted_filter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="@string/filter_accepted_button"
        android:textSize="24sp" />
</LinearLayout>