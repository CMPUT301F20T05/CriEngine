<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.criengine">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.FilterFragmentActivity"></activity>
        <activity android:name=".Activities.MyBooksActivity" />
        <activity android:name=".Activities.RequestActivity"></activity>
        <activity android:name=".Activities.MainActivity">
        <activity android:name=".Activities.MyBookActivity"></activity>
        <activity android:name=".Activities.BookActivity" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.MyProfileActivity"
            android:parentActivityName=".Activities.MainActivity" />
    </application>

</manifest>